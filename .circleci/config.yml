version: 2.1
jobs:
  build:
    docker:
      - image: circleci/golang:1
        environment:
          GO111MODULE: "on"
    working_directory: /go/src/github.com/martinsirbe/prometheus-graphite-demo
    steps:
      - checkout
      - run:
          name: Download project dependencies.
          command: go mod download
      - run:
          name: Run golangci-lint.
          command: |
            go get -u github.com/golangci/golangci-lint/cmd/golangci-lint
            golangci-lint run -v
      - run:
          name: Run tests.
          command: make test
      - setup_remote_docker:
          version: 18.09.3
      - run:
          name: Build a docker image.
          command: |
            docker build \
              -t martinsirbe/prometheus-graphite-demo:$(git rev-parse --short $CIRCLE_SHA1) \
              -t martinsirbe/prometheus-graphite-demo:latest \
              -f Dockerfile .
      - run:
          name: Push the docker image to docker hub.
          command: |
            echo "$DOCKERHUB_PASSWORD" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin
            docker push martinsirbe/prometheus-graphite-demo:$(git rev-parse --short $CIRCLE_SHA1)
            docker push martinsirbe/prometheus-graphite-demo:latest
workflows:
  version: 2
  commit:
    jobs:
      - build:
          filters:
            branches:
              only: master
  periodic-build:
    triggers:
      - schedule:
          cron: "0 0 * * 0"
          filters:
            branches:
              only:
                - master
    jobs:
      - build
